YUI.Env[Y.version].groups = YUI.Env[Y.version].groups || {};
YUI.Env[Y.version].groups.gallery = {
    base: Y.config.base,

    patterns: {
        'gallery-': {},
        'lang/gallery-': {},
        'gallerycss-': { type: 'css' }
    }
};
